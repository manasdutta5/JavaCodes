package DataStructures.Arrays;

/*
// Find start and end indexes of continuous subarray with given sum
// store both in an arraylist and return
// If no such array exists, return arraylist containing (-1)
//
//
// Problem link: https://practice.geeksforgeeks.org/problems/subarray-with-given-sum-1587115621/1?page=1&difficulty[]=0&category[]=Arrays&sortBy=submissions
//
//********************** APPROACH *****************************
// Use two pointer, left and right.
// Place both initially at starting index 0
// initialise current sum = 0, move right pointer towards right
// For each iteration:
// Add element at index 'right' to currentSum.
// Check if sum equals given sum, if yes, append both index to arraylist, and return.
// If currrentSum < Sum, keep repeating above step
// If currentSum > Sum, subtract element at left pointer from currentSum and move
// left pointer forward by one
 */




public class SubarrayWithGivenSum {
    public static void main(String[] args) {

    }
}
